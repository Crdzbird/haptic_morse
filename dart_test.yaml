tags:
  # Tests that need the Flutter SDK (they drive a MethodChannel). Everything
  # untagged must run under the plain Dart SDK, which is what proves the core
  # library stays free of Flutter and dart:ui.
  flutter:
